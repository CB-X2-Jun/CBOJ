name = "cboj-worker"
main = "src/index.ts"          # 告诉系统入口文件在这里！
compatibility_date = "2026-08-21"

[[d1_databases]]
binding = "DB"                 # 绑定名必须和代码里的一致
database_name = "cboj-db"      # 这里写名字只是为了占位，实际 ID 靠 UI 绑定覆盖
# 注意：不要在这里写 database_id，留空让 UI 绑定生效
[vars]
ADMIN_UID_LIST = "1,2,3"
GITHUB_OWNER = "CB-X2-Jun"
GITHUB_REPO = "CBOJ"
SITE_URL = "https://ojapi.x2cb.cc.cd"